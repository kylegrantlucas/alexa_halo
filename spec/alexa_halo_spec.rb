require 'spec_helper'

describe AlexaHalo do
  it 'has a version number' do
    expect(AlexaHalo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
